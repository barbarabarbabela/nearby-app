import { View } from "react-native";
import { PlaceProps } from "../place";

type Props = {
    data: PlaceProps[]
}

export function Places({ data }: Props) {
    return (
        <View></View>
    )
}